<?xml version="1.0" encoding="utf-8"?>
<selector
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- when the button is enable, green color.-->
    <item android:state_enabled = "true">
       <shape android:shape="rectangle"  >
            <corners android:radius="2dip" />
            <stroke android:width="1dip" android:color="#00D666" />
           <solid android:color="#00D666" />
           <gradient
               android:angle="-90"
               android:startColor="#00D666"
               android:endColor="#00D666" />

       </shape>
    </item>

<!-- when the button is not enable, gray color.-->
    <item android:state_enabled = "false">
        <shape>
            <gradient
                android:endColor="@android:color/darker_gray"
                android:startColor="@android:color/darker_gray"
                android:angle="270" />
            <stroke
                android:width="3dp"
                android:color="@android:color/darker_gray" />
            <corners
                android:radius="2dip" />
        </shape>
    </item>
</selector>